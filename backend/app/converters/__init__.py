"""VT model converters that emit EcoTEA Excel files.

This package was lifted from the standalone ``vt_ecotea_tool`` so that the
DBDesign service can run the full pipeline (convert -> import -> browse) in a
single backend.
"""
